# 🎯 QR Code Generator (Python)

This simple Python script generates a **QR code** for any given URL or text using the [`qrcode`](https://pypi.org/project/qrcode/) library.

---

## 📌 Features
- Generate a QR code for any URL or text.
- Customize the **fill color** and **background color**.
- Save the QR code as an image file (`.png`).

---

## 🚀 Requirements
Make sure you have Python installed, then install the required library:

```bash
pip install qrcode[pil]
